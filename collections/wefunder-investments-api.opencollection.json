{
  "opencollection": "1.0.0",
  "info": {
    "name": "Wefunder API v2 Activity Investments API",
    "version": "2.0"
  },
  "items": [
    {
      "info": {
        "name": "Investments",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "List user investments",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "https://{environment}.wefunder.com/api/v2/investments",
            "params": [
              {
                "name": "cursor",
                "value": "12345",
                "type": "query",
                "description": "Cursor-based pagination token. Use the `next_cursor` value from the previous response's\n`meta` object to retrieve the next page of results. Omit this parameter to retrieve the\nfirst page.\n"
              }
            ],
            "auth": {
              "type": "oauth2",
              "flow": "client_credentials",
              "accessTokenUrl": "https://wefunder.com/oauth/token",
              "credentials": {
                "clientId": "{{clientId}}",
                "clientSecret": "{{clientSecret}}"
              }
            }
          },
          "docs": "Retrieves a paginated list of all investments made by the authenticated user. Each investment\nincludes details about the amount invested, the company, investment date, and current status.\n\nUse this endpoint to:\n- Display a user's investment portfolio\n- Track investment history and performance\n- Build custom reporting and analytics\n\nThe list is ordered by investment date (newest first) and supports cursor-based pagination\nfor efficient iteration through large portfolios. Each investment object in"
        }
      ]
    }
  ],
  "bundled": true
}